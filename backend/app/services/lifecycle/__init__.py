"""Lifecycle messaging integration (Loops CRM).

We emit events + contact properties to Loops (loops.so); Loops owns all
campaign timing, copy, templating, and delivery. See docs/loops-campaigns.md
for the campaigns that consume these events.
"""
