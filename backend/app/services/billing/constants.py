"""Constants for the billing subsystem."""

# Note: the generic system-actor UUID used when writing audit entries from
# webhook code lives at `app.services.core.audit.SYSTEM_ACTOR_ID` so that
# other system processes (scheduled jobs, imports) can reuse it.
